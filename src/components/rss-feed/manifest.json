{
	"id": "rss-feed",
	"name": "RSS Feeds",
	"version": "1.0.0",
	"description": "Display news articles from RSS feeds",
	"config": {
		"title": "RSS Feeds Settings",
		"description": "Add any public RSS/Atom feed URLs from your favourite news providers. You can usually find these on the publisher's \"RSS\" or \"Feeds\" page.",
		"fields": [
			{
				"key": "rssFeeds",
				"type": "array",
				"label": "RSS Feeds",
				"description": "Each entry defines one source. Use https links when available to avoid mixed-content issues.",
				"itemFields": [
					{
						"key": "sourceName",
						"type": "text",
						"label": "Feed Name",
						"description": "Display name for this RSS feed",
						"placeholder": "e.g., The Guardian | World"
					},
					{
						"key": "url",
						"type": "text",
						"label": "Feed URL",
						"description": "RSS feed URL",
						"placeholder": "https://example.com/feed.xml"
					}
				]
			}
		]
	}
}
