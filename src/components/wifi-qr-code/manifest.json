{
	"id": "wifi-qr-code",
	"name": "WiFi QR Code",
	"version": "1.0.0",
	"description": "Display WiFi network QR code for easy connection",
	"config": {
		"title": "WiFi Settings",
		"description": "Enter the WiFi credentials exactly as broadcast by your router. MMM generates a QR code that mobile devices can scan to join instantly.",
		"fields": [
			{
				"key": "networkName",
				"type": "text",
				"label": "Network Name (SSID)",
				"description": "SSID as shown in your WiFi settings (case-sensitive).",
				"placeholder": "Enter network name"
			},
			{
				"key": "password",
				"type": "password",
				"label": "Password",
				"description": "WiFi passphrase. Leave blank only if the network is open.",
				"placeholder": "Enter WiFi password"
			},
			{
				"key": "securityType",
				"type": "select",
				"label": "Security Type",
				"description": "Select the security protocol that matches your router configuration.",
				"default": "WPA",
				"options": [
					{ "value": "WPA", "label": "WPA/WPA2" },
					{ "value": "WEP", "label": "WEP" },
					{ "value": "nopass", "label": "No Password" }
				]
			}
		]
	}
}
